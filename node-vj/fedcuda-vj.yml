---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: fedcuda-pv-vj
  labels:
    type: local
spec:
  capacity:
    storage: 1Ti
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /home2/jennyk0321/2024_data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fedcuda-pvc-vj
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  storageClassName: manual
  selector:
    matchLabels:
      type: local
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fedcuda-deployment-vj
  annotations:
    kubernetes.io/change-cause: "docker image installed torchvision as v0.2"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fedcuda-vj
  template:
    metadata:
      labels:
        app: fedcuda-vj
    spec:
      nodeSelector:
        key: vj
      containers:
      - name: fedcuda-vj
        image: dwnusa/fedcuda:v0.2
        ports:
          - containerPort: 22
        resources:
          requests:
            nvidia.com/gpu: 1
          limits:
            nvidia.com/gpu: 1
        volumeMounts:
        - name: fedcuda-volume
          mountPath: /root/data
      volumes:
      - name: fedcuda-volume
        persistentVolumeClaim:
          claimName: fedcuda-pvc-vj
---
apiVersion: v1
kind: Service
metadata:
  name: fedcuda-nodeport-vj
spec:
  type: NodePort
  selector:
    app: fedcuda-vj
  ports:
    - protocol: TCP
      port: 22
      targetPort: 22
      nodePort: 30022
